<div class="version-select">
  <ul>
    <li class="version-select<%= @version.nil? ? '-current' : '' %>">
      <%= link_version_select(:all) %>
    </li>
<% @versions.each do |version| %>
    <li class="version-select<%= @version == version.key ? '-current' : '' %>">
      <%= link_version_select(version.key) %>
    </li>
<% end %>
  </ul>
</div>

<div class="statistics">
  <span class="total"><%= h("#{@n_entries}件ヒット") %></span>
  <span class="elapsed-time"><%= h("（%.3f秒）" % @elapsed_time) %></span>
</div>

<% if @n_entries.zero? %>
<div class="no-result">
  <p>見つかりませんでした。検索条件を変えてみませんか？</p>
</div>
<% else %>
<div class="result">
<%= paginate %>
  <table class="entries">
    <thead>
      <tr>
        <th>名前</th>
        <th>種類</th>
        <th>バージョン</th>
        <th>内容</th>
      </tr>
    </thead>
    <tbody>
<%  @entries.each_with_index do |entry, i| %>
      <tr class="entry-<%= i.odd? ? 'odd' : 'even' %>">
        <td class="entry-name"><%= link_entry(entry) %></td>
        <td class="entry-type"><%= link_type(entry) %></td>
        <td class="entry-version"><%= link_version(entry) %></td>
        <td class="entry-description"><%= format_description(entry) %></td>
      </tr>
<%  end %>
    </tbody>
  </table>
<%= paginate %>
</div>
<% end %>
